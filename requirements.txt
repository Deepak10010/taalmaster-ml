# Core ML
xgboost==2.1.3
scikit-learn==1.6.1
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1

# Database — Neon Postgres (still used for prediction logging back to the live app DB).
psycopg2-binary==2.9.10
sqlalchemy==2.0.36

# BigQuery — primary data source as of step 8 of the data platform refactor.
# `db-dtypes` is required for .to_dataframe() to render TIMESTAMP/DATE columns.
google-cloud-bigquery==3.27.0
db-dtypes==1.3.1
# Pin google-api-core + protobuf + grpcio to stay <6 so mlflow 2.19.0 (protobuf<6) keeps working.
# google-api-core 2.25+ and grpcio-status 1.72+ widened their caps to allow protobuf 6, which breaks mlflow.
google-api-core==2.24.2
protobuf==5.29.6
grpcio==1.71.0
grpcio-status==1.71.0

# API
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Experiment tracking
mlflow==2.19.0

# Utilities
python-dotenv==1.0.1
joblib==1.4.2
pydantic==2.10.4

# Data & pipeline versioning
dvc==3.67.1
# S3-compatible remotes (Cloudflare R2, AWS S3, Backblaze B2, MinIO, ...)
dvc-s3==3.2.2
# MLflow uses boto3 directly (not the dvc-s3 plumbing) when MLFLOW_S3_ENDPOINT_URL is set.
# 1.42.91 chosen to stay compatible with aiobotocore (transitive dep of dvc-s3),
# which at time of writing caps botocore at <1.42.92. Bumping boto3 above that
# breaks the pip resolver.
boto3==1.42.91

# Testing
pytest==8.3.4
