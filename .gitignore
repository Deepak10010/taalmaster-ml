__pycache__/
*.pyc
.env
mlruns/
artifacts/
*.joblib
.venv/

# DVC-tracked outputs: DVC manages these (the .dvc pointers are what git tracks)
data/
logs/

# DVC local-only config holds secrets (R2 / S3 credentials). Never commit.
.dvc/config.local
.dvc/tmp/
.dvc/cache/

